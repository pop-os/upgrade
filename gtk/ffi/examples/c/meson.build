project('example', 'c')

dbus = dependency('dbus-1')
gtk = dependency('gtk+-3.0')
s76 = dependency('pop_upgrade_gtk')
threads = dependency('threads')

executable('example', 'main.c', dependencies : [ dbus, gtk, s76, threads ])